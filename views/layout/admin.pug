doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Bienes Raices | #{page}
        block styles
        link(rel="stylesheet", href="/css/app.css", attr)
    body 
        if bar
            include incluides/header 
        main(class="mx-auto container mt-10 px-2")
            block container
        block  scripts

